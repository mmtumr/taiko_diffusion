data:
  cache_dir: data/cache/diffusion_v15_full_catalog
  audio_cache_dir: data/cache/audio_v15_full_catalog
  context_channels: []

model:
  audio_channels: 66
  condition_dim: 11
  query_count: 64
  hidden_dim: 192
  context_tokens: 512
  encoder_layers: 3
  decoder_layers: 3
  attention_heads: 6
  dropout: 0.1

loss:
  exist_positive_weight: 8.0
  exist_weight: 1.0
  start_weight: 4.0
  start_regression_weight: 0.5
  duration_weight: 4.0
  count_weight: 2.0
  order_weight: 1.0

training:
  seed: 20260723
  batch_size: 16
  epochs: 20
  learning_rate: 0.0001
  weight_decay: 0.00001
  num_workers: 0
  checkpoint_dir: checkpoints/hold_span_v2_deploy
